# Include Directory

Public headers for compiler components.

Current headers:

- `include/omega/compiler.h` - shared compiler structs, enums, and stage APIs.
- `include/omega/modules.h` - module and builtin registry interface.
