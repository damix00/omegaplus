import sti.
import vector.

-> main() {
  int32 n.
  sti<->input(n).

  vector<-int32 v.

  int32 i = n.
  while(i > 0i) {
    int32 x.
    sti<->input(x).

    v.push_back(x).
    i = i - 1i.
  }

  sti<->print(v[0i]).
}
